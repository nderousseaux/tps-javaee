2) Créer une servlet qui renvoie une liste d’objets à l’utilisateur au clic d’un bouton. Le résultat doit apparaître sous la forme d’une liste déroulante.

3.2) Créer une servlet qui permet de simuler une connexion à un site (login/mot de passe). Si l’utilisateur entre les bons identifiants, le rediriger vers une nouvelle page affichant son nom. Dans le cas contraire, faire apparaître une fenêtre indiquant l'erreur de connexion.

4) Créer une servlet qui ajoute une liste d'objets dans la requête reçue, puis transmet la requête vers une page JSP qui affiche cette liste d'objets dans une liste déroulante. Utiliser RequestDispatcher.

5) Écrire une servlet principale qui redirige vos requêtes vers vos autres servlets (tous les anciens exercices doivent utiliser cette servlet).

6) Écrire une servlet qui peut :

    enregistrer un objet Java dans un cookie
    supprimer le cookie

L'interface graphique doit être composée d’un bouton qui permet de créer le cookie et d’un bouton qui permet de le supprimer. Changer l’affichage de la page JSP si le cookie existe déjà.

7) Reprendre la servlet de l'exercice 3. En cas d'erreur de connexion, rediriger l'utilisateur vers la page d'accueil et afficher un message d'erreur. Lorsque l'utilisateur se connecte, enregistrer son identifiant dans la session et le rediriger vers un page listant un ensemble d'articles dans un tableau. L'utilisateur peut ajouter ces articles dans un panier, consulter le panier, supprimer un des articles ou le vider. Chaque article est représenté par un numéro, un libellé, une image et un prix. Lors de l'affichage du panier, le prix total est affiché. L'utilisateur doit aussi avoir la possibilité de modifier la quantité de chaque article dans le panier.

Bien respecter le modèle MVC vu en cours.

8) Créer un filtre qui permet de vérifier qu’un utilisateur est authentifié. Modifier la servlet de l’exercice 7 :

        Lorsque l’utilisateur se connecte : créer une session ;

        le filtre devra utiliser cette session pour savoir si l'utilisateur est authentifié ; s'il ne l'est pas, renvoyer une erreur “Access Forbiden” (erreur 403).

